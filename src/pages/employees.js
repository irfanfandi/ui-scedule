import Layout from "@/sections/Layout";
import { Toolbar, Typography } from "@mui/material";

export default function Employees() {
  return (
    <Layout>
      <Toolbar />
      <Typography>Page Employees</Typography>
    </Layout>
  );
}
