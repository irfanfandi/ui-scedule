import Layout from "@/sections/Layout";
import { Toolbar, Typography } from "@mui/material";

export default function Schedule() {
  return (
    <Layout>
      <Toolbar />
      <Typography>Page Schedule</Typography>
    </Layout>
  );
}
