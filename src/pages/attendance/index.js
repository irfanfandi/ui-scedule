import Layout from "@/sections/Layout";
import { Toolbar, Typography } from "@mui/material";

export default function Attendance() {
  return (
    <Layout>
      <Toolbar />
      <Typography>Page Attendance</Typography>
    </Layout>
  );
}
