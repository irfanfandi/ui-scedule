import Layout from "@/sections/Layout";
import { Toolbar, Typography } from "@mui/material";

export default function Payroll() {
  return (
    <Layout>
      <Toolbar />
      <Typography>Page Payroll</Typography>
    </Layout>
  );
}
