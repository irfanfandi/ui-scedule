import Layout from "@/sections/Layout";
import { Toolbar, Typography } from "@mui/material";

export default function Leave() {
  return (
    <Layout>
      <Toolbar />
      <Typography>Page Leave</Typography>
    </Layout>
  );
}
